generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Extend with core models (User, Game, etc.) as tasks progress.
model Game {
  id        String  @id @default(uuid())
  createdAt DateTime @default(now())
}
